# Final GIF slots

The public README already uses real static captures. Replace the three marked image paths
when the final recordings are ready:

1. `hero-workspace.gif` — the full vertical workspace and native widgets.
2. `window-to-widget.gif` — selecting an open app, embedding it, moving and resizing it.
3. `canvas-pages.gif` — a slow mouse gesture between workspace pages.

Keep each GIF under roughly 10 MB where possible and record at the same aspect ratio as
its current PNG. The exact replacement comments are present in `README.md`.
